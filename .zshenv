# загружается единожды при логине
# ~/.zprofile в отличие от %subj% загружается только при логине через консоль 
# или ssh
# здесь следовало бы объявлять переменные окружения, если бы это не было
# удобнее делать в ~/.zshrc
typeset -U path
path=(~/.local/bin ~/bin $path[@])
